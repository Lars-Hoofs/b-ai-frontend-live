import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
    ],
  },
  // Note: rewrites don't forward cookies properly in all cases
  // We'll use a custom API route handler instead
};

export default nextConfig;
